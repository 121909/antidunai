# Telegram 群组视频刷屏治理机器人开发计划

## 1. 项目目标

基于 `parse_hub_bot` 的消息解析流程增加群组防刷屏能力：当指定用户在同一段连续会话中发送至少 3 条视频链接或视频文件时，随机只保留 1 条，并同步删除其余源消息及对应的机器人解析结果。

本项目直接在 `antidunai` 仓库内开发和维护。`parse_hub_bot` 仅作为上游代码与实现参考，不向其原仓库提交或推送任何代码。

## 2. 仓库与推送约束

- 唯一允许提交和推送实现代码的仓库是 `antidunai`：`https://github.com/121909/antidunai`。
- 禁止向 `z-mio/parse_hub_bot` 的任何分支推送代码、提交 Pull Request 或改写其 Git 历史。
- 如需同步上游代码，仅允许读取、拉取或以只读方式添加上游远端。
- 如果本地需要配置上游远端，应将其命名为 `upstream`，并使用不具备写权限的 URL；推送前必须执行 `git remote -v` 确认目标为 `antidunai` 的 `origin`。
- 不在自动化脚本中执行隐式 `git push`。发布操作必须显式指定 `git push origin <branch>`。
- 上游更新通过 `fetch` 后在 `antidunai` 的功能分支中合并或变基，冲突解决和适配提交仍只保存在 `antidunai`。

## 3. 需求口径

- “两条以上且不含两条”按至少 3 条触发。
- 一条 Telegram 消息即使包含多个视频链接，也只计为 1 条。
- 同一目标用户发送的普通文本暂定不打断连续状态。
- 解析机器人发送的消息不打断连续状态。
- 任何其他真人用户发送消息后，当前连续状态结束。
- 机器人仅在群组和超级群组中执行治理，私聊和内联解析不参与。
- 目标用户可以通过固定 Telegram 用户 ID 或不区分大小写的 `@username` 配置；用户 ID 优先。
- 视频文件包括 Telegram `video`、`video_note` 以及 MIME 类型为 `video/*` 的文档。
- 源链接已被用户自行删除时，仍需删除与其关联的解析结果。

## 4. 技术方案

直接扩展并自行部署解析机器人，而不是另建普通的审核 Bot。普通 Telegram Bot 无法可靠接收其他 Bot 发出的消息，独立审核 Bot 因而无法稳定建立“源消息 -> 解析结果”关联。

整体处理流程如下：

1. 在群消息进入解析处理前识别目标用户及候选视频消息。
2. 按群组建立连续段，并为候选消息分配状态记录。
3. 解析过程中持续登记机器人发送的进度消息、视频、媒体组、文档和文本结果。
4. 连续段达到第 3 条时随机保留 1 条，批量删除其他候选项的源消息和解析结果。
5. 后续候选消息通过蓄水池抽样决定是否替换当前保留项，确保连续段中的每条候选消息拥有相同的最终保留概率。
6. 其他真人用户发言后关闭当前连续段，新消息从新的连续段重新计数。

## 5. 配置设计

在 `core/config.py` 的 `BotSettings` 中增加以下环境变量：

- `BURST_GUARD_ENABLED`：是否启用防刷屏功能，默认关闭。
- `BURST_GUARD_TARGETS`：目标用户 ID 或用户名列表，例如 `123456789,@username`。
- `BURST_GUARD_THRESHOLD`：触发阈值，默认值为 `3`。
- `BURST_GUARD_VIDEO_PLATFORMS`：计入治理的视频平台 ID 列表，例如 `douyin,bilibili,youtube`。

同时更新 `.env.exa` 和 `README.md`，说明配置格式、用户名变更风险、权限要求及启用方式。

视频链接使用现有 `ParseService().parser.get_platform()` 识别，并通过视频平台白名单限制范围。对同时支持图文和视频的平台，基于平台识别可能将图文链接计入候选；首个版本通过白名单控制，后续可增加解析结果级别的严格视频类型确认。

## 6. 状态服务

新增 `services/burst_guard.py`，负责并发安全的连续段和候选项管理。

核心数据结构：

- `BurstRun`：群组 ID、目标用户 ID、连续段编号、候选数量、当前保留项、关闭状态。
- `BurstItem`：源消息 ID、用户 ID、解析任务集合、已发送消息 ID 集合、保留或淘汰状态。
- `OutputTracker`：登记解析过程中产生的每一条机器人消息，并在候选项已淘汰时立即补删。

状态仅保存在内存中。机器人重启后清空状态，不追溯处理历史消息。每个群组只保留当前连续段及仍有解析任务运行的已关闭候选项，任务结束后立即释放，避免长期占用内存。

所有候选登记、随机选择、输出消息登记和状态切换均通过群组级 `asyncio.Lock` 串行化，Telegram API 删除操作放在锁外执行，避免网络等待阻塞后续消息处理。

## 7. 随机保留算法

- 第 1、2 条候选消息只登记，不删除。
- 第 3 条到达时，从 3 条候选消息中等概率随机选择 1 条作为保留项，其余 2 条进入淘汰状态。
- 第 `n` 条（`n > 3`）到达时，以 `1/n` 的概率用新候选替换当前保留项，否则淘汰新候选。
- 被替换的旧保留项立即进入淘汰状态。
- 随机数生成器通过依赖注入提供，生产环境使用标准随机源，测试环境使用固定随机源以验证全部分支。

该蓄水池抽样方式不需要恢复已经删除的消息，并能保证连续段中每条候选最终被保留的概率均为 `1/n`。

## 8. 消息监听

新增 `plugins/burst_guard.py`，使用早于解析 handler 的处理组监听群消息：

- 目标用户发送符合条件的视频链接或视频文件时，创建或加入当前连续段。
- 同一用户发送普通消息时，按当前需求口径保持连续段。
- 其他真人用户发言时关闭当前连续段。
- 机器人消息、服务消息和解析进度消息既不作为候选，也不打断连续段。
- 匿名管理员或以 `sender_chat` 身份发送的消息按其他用户消息处理，避免错误跨段。
- 不同群组的状态完全隔离；另一名目标用户发言也会打断当前用户的连续段并开始自己的新连续段。

## 9. 解析结果关联

调整 `plugins/parse.py`，在不改变原有解析能力的前提下接入候选项和输出追踪：

- 使用 `(chat_id, source_message_id)` 查找候选项。
- 一条源消息包含多个链接时，所有解析任务和输出均归入同一个候选项，但候选计数仍为 1。
- 所有 `reply_*` 调用返回的 `Message` 或消息列表必须交给 `OutputTracker` 登记。
- 覆盖缓存命中、普通解析、富文本、单媒体、媒体组、GIF、兼容文档、Raw、Zip、进度和错误消息。
- 候选项被淘汰后，取消尚未完成的解析任务并清理下载产物。
- 如果任务取消与 Telegram 发送并发发生，后来成功发送的消息由 `OutputTracker` 立即删除。
- 对保留项不改变现有解析和发送行为。

## 10. 删除策略

- 淘汰项的删除集合包括源链接或源视频、全部解析结果、媒体组内每条消息，以及尚未清理的进度或错误消息。
- 使用客户端批量删除接口，在 Telegram 单次限制内分批提交消息 ID。
- 源消息已被用户删除或消息不存在时视为幂等成功，不中断其他消息的清理。
- `FloodWait` 按服务端给出的等待时间重试。
- 权限不足、消息 ID 无效等错误写入结构化日志，不影响其他候选项和群组。
- 不通过内容或时间范围猜测关联关系，只删除 `OutputTracker` 明确登记的消息。
- 不删除解析机器人发送的无关消息。

“同步删除”在解析结果已经存在时通过同一批删除请求完成；解析结果尚未发送时先淘汰并取消任务，竞态下产生的迟到消息立即补删。

## 11. 测试计划

新增 `tests/` 并覆盖以下场景：

- 发送 1 条或 2 条时不删除，第 3 条后仅保留 1 条。
- 第 4 条及以后始终仅保留 1 条。
- 使用固定随机源验证保留旧项、替换为新项等分支。
- 其他真人消息打断连续段，机器人消息不打断。
- 同一目标用户普通文本不打断连续段。
- 多群组、多目标用户和多个连续段之间状态隔离。
- 用户 ID、用户名、大小写、前导 `@` 和缺失用户名的匹配。
- 视频、视频便笺、视频文档、单链接、多链接和非视频消息识别。
- 缓存命中、正常解析、媒体组、兼容文档、Raw、Zip 和解析失败路径。
- 源消息已被用户删除、解析结果延迟发送、取消任务与发送并发等竞态。
- 删除权限不足、无效消息 ID 和 Telegram 限流。
- 功能关闭或目标用户未匹配时，原有解析行为完全不变。

测试分层：

1. 状态服务单元测试，不调用 Telegram API。
2. 使用伪造 `Client` 和 `Message` 的 handler 集成测试。
3. 在测试群进行真实 Telegram API 验收。
4. 执行现有静态检查，并补充 `ruff` 和 `mypy` 检查。

## 12. 部署与验收

部署前要求：

- 使用 `antidunai` 仓库构建并部署自有机器人实例。
- 机器人在目标群内拥有删除消息权限。
- 关闭 BotFather 隐私模式，或确保机器人作为管理员可以接收完整群消息。
- 首次部署保持 `BURST_GUARD_ENABLED=false`，确认原有解析功能正常后再开启。

测试群验收用例：

1. 连续发送 2 条视频链接，确认均保留。
2. 连续发送第 3 条，确认最终仅保留一个完整的“源消息 + 解析结果”组合。
3. 继续发送第 4、5 条，确认群内仍只保留一个组合。
4. 由其他群友插入消息，确认后续重新计数。
5. 目标用户主动删除源链接，确认淘汰时解析结果仍会被删除。
6. 验证缓存解析、非缓存解析和媒体组结果。
7. 重启机器人，确认旧连续段不会被继续处理。

## 13. 实施顺序

- [x] 1. 在 `antidunai` 创建功能分支 `feature/burst-video-guard`。
- [x] 2. 将所需上游代码引入 `antidunai`，并记录对应的 `parse_hub_bot` 上游提交版本。
  - 上游基线：`z-mio/parse_hub_bot@9c86020db075f207c270993b1e7671576e14da56`（`main`）。
- [x] 3. 完成配置模型、状态服务和单元测试。
- [x] 4. 完成群消息监听与连续段识别。
- [ ] 5. 重构解析发送路径并接入 `OutputTracker`。
- [ ] 6. 完成批量删除、取消任务、异常处理和日志。
- [ ] 7. 执行单元测试、集成测试、`ruff` 与 `mypy`。
- [ ] 8. 在测试群完成真实 Telegram API 验收。
- [ ] 9. 检查 `git diff`、配置示例和文档，确认没有 Token、API Hash 或群组隐私数据。
- [ ] 10. 执行 `git remote -v`，确认推送目标为 `antidunai` 的 `origin`。
- [ ] 11. 仅执行显式的 `git push origin feature/burst-video-guard`，禁止向 `parse_hub_bot` 上游推送。

## 14. 完成标准

- 所有需求口径均有自动化测试覆盖。
- 连续 3 条及以上候选消息最终只保留 1 条。
- 被淘汰源消息与所有关联解析结果均被删除或记录明确的权限错误。
- 并发解析和延迟发送不会产生长期残留消息。
- 功能默认关闭，未启用时不改变原有机器人行为。
- 配置、部署权限、已知限制和排障方式已写入文档。
- 所有开发提交仅存在于 `antidunai` 仓库，且推送目标经过人工确认。
