# burst-guard 独立 userbot 服务开发计划

## 1. 项目目标

开发一个独立部署的 Telegram 群组治理服务 `burst-guard`，运行形态为专用用户账号自动化程序（下文简称 userbot）。

每个群组按非机器人消息顺序切分为固定的 `X` 条消息一组。当一组内指定用户的视频候选消息至少达到 `Y` 条时，服务从该组全部候选消息中随机保留 1 条，并删除其余候选消息。服务只负责 Telegram 群消息识别、状态管理和删除操作，不下载媒体内容，也不依赖其他业务服务。

首个版本仅在群组和超级群组中运行，使用专门注册的 Telegram 用户账号作为自动化账号。该账号不得与个人日常账号混用，必须是受管群组的管理员，并具有删除消息权限。

## 2. 需求口径

### 2.1 候选消息

指定用户发送的以下消息视为候选并计入 `Y`：

- Telegram `video`。
- Telegram `video_note`。
- MIME 类型为 `video/*` 的文档。
- 文本或说明文字中包含已配置视频域名 URL 的消息。

补充规则：

- 一条 Telegram 消息即使包含多个 URL，也只计为 1 条候选消息。
- 媒体组中的每一条 Telegram 消息分别计数。
- URL 只按域名识别，不发起外部网络请求。
- 短链接域名必须显式加入配置，否则不计入候选。
- 首个版本只处理新消息；消息编辑不改变已经登记的候选状态。

### 2.2 固定分组

每个群组同时最多存在一个活动固定分组：

- 目标用户和其他真人用户的每条新消息都占用 1 个分组位置。
- 匿名管理员或以频道身份发送的消息也占用 1 个分组位置。
- 机器人账号消息和 Telegram 服务消息不占用分组位置；机器人上传的视频如果携带已登记候选的原链接，则作为候选输出关联，用于同步清理淘汰项。
- 分组累计到 `X` 条非机器人消息时立即结算并关闭，下一条非机器人消息创建新组；各组互不重叠。
- 不同群组的计数和候选完全隔离。
- 服务重启后清空未完成分组，不追溯历史消息。

`X` 和 `Y` 均由环境变量配置，只要求 `X >= Y >= 1`。

### 2.3 保留规则

- 分组未满 `X` 条时只登记，不执行随机删除。
- 分组结算时，如果候选少于 `Y` 条，则全部永久保留。
- 分组结算时，如果候选至少为 `Y` 条，则从该组全部候选中等概率随机保留 1 条，删除其余候选。
- 结算后不修改上一组结果，也不把上一组消息带入下一组。

## 3. 范围边界

### 3.1 首个版本包含

- 多群组管理。
- 多个目标用户。
- 视频域名 URL 识别及解析机器人视频输出关联。
- 固定消息分组状态管理。
- 等概率随机保留。
- 批量删除和限流重试。
- 试运行模式。
- 启动前权限检查。
- 结构化日志和基础健康检查。
- Docker Compose 部署。
- 单元测试、处理器集成测试和真实群验收脚本。

### 3.2 首个版本不包含

- 媒体下载、转码或内容识别。
- 根据页面正文判断链接实际内容类型。
- Web 管理后台。
- 数据库和重启后的未完成分组恢复。
- 多实例并行消费 Telegram 更新。
- 根据时间、内容相似度或相邻消息猜测关联关系。
- 私聊、频道和内联模式治理。

## 4. 技术选型

- Python 3.12。
- Telethon 1.x。
- Telegram MTProto 持久连接与实时更新。
- asyncio 并发模型。
- Pydantic Settings 或等价的显式环境变量校验层。
- uv 负责依赖和锁文件。
- pytest、pytest-asyncio、ruff 和 mypy。
- Docker 和 Docker Compose。

首个版本只允许启动一个实例。同一个用户会话文件不得被多个进程或容器同时使用，内存状态也无法跨实例保持一致。需要水平扩展时，必须先设计独立会话、共享状态存储和分布式锁，不允许直接复制当前服务。

## 5. 服务架构

```text
Telegram updates
       |
       v
Update Router
       |
       +--> Chat / sender filter
       |
       +--> Candidate classifier
       |
       +--> Burst state service
                  |
                  +--> Cleanup requests
                              |
                              v
                      Telegram MTProto API
```

模块职责：

- `config`：读取、标准化和校验环境变量。
- `classifier`：识别视频媒体、允许域名 URL 和用于关联的机器人视频媒体。
- `state`：维护群组固定消息分组、随机保留和原链接关联状态。
- `handlers`：把 Telegram 更新转换为状态事件。
- `cleanup`：在状态锁之外执行删除、分批和重试。
- `preflight`：检查账号、群类型和管理员权限。
- `health`：提供存活和就绪状态。

核心业务模块不直接依赖 Telethon 的复杂对象。处理器先把 Telegram 消息转换成内部数据模型，便于测试状态算法。

## 6. 配置设计

建议环境变量：

```dotenv
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_EXPECTED_USER_ID=
TELEGRAM_SESSION_PATH=/app/data/burst-guard.session
BURST_GUARD_ENABLED=false
BURST_GUARD_DRY_RUN=true
BURST_GUARD_CHAT_IDS=-1001234567890
BURST_GUARD_TARGETS=123456789,@example_user
BURST_GUARD_GROUP_SIZE=10
BURST_GUARD_THRESHOLD=3
BURST_GUARD_VIDEO_DOMAINS=youtube.com,youtu.be,bilibili.com,douyin.com,tiktok.com
BURST_GUARD_DELETE_BATCH_SIZE=100
BURST_GUARD_DELETE_RETRY_LIMIT=3
BURST_GUARD_IDEMPOTENCY_TTL_SECONDS=3600
BURST_GUARD_LOG_LEVEL=INFO
BURST_GUARD_HEALTH_HOST=0.0.0.0
BURST_GUARD_HEALTH_PORT=8080
```

校验规则：

- `TELEGRAM_API_ID` 必须是正整数。
- `TELEGRAM_API_HASH` 必填，日志中不得输出。
- `TELEGRAM_EXPECTED_USER_ID` 必须是专用账号的数字用户 ID。
- `TELEGRAM_SESSION_PATH` 必须指向有效的专用用户账号会话文件。
- `BURST_GUARD_CHAT_IDS` 至少包含一个群组 ID。
- `BURST_GUARD_TARGETS` 至少包含一个用户 ID 或用户名。
- 用户 ID 优先；用户名去掉前导 `@` 后按大小写不敏感比较。
- `BURST_GUARD_GROUP_SIZE >= BURST_GUARD_THRESHOLD >= 1`。
- 域名统一转成小写，移除端口和末尾的点。
- 删除批大小限制为 `1..100`。
- 列表环境变量使用自定义 CSV 读取器，不交给通用 JSON 解码流程。
- 非法配置在启动阶段明确报错并退出，不使用静默默认值掩盖错误。
- 自动化账号自身不得出现在 `BURST_GUARD_TARGETS` 中。

生产环境应优先配置稳定的数字用户 ID。用户名可以变更，仅作为兼容方式。

### 6.1 用户会话初始化

用户会话只允许通过单独的初始化命令创建：

1. 为服务准备一个专用 Telegram 用户账号，并启用两步验证。
2. 在可信终端运行 `python -m burst_guard.login`。
3. 交互输入手机号、Telegram 验证码和两步验证密码。
4. 将生成的会话文件写入 `TELEGRAM_SESSION_PATH`，权限设置为 `0600`。
5. 把专用账号加入受管群组，授予管理员和删除消息权限。
6. 停止初始化进程，再启动常驻服务。

登录初始化可通过独立 Compose profile 执行，但不得与常驻服务同时运行。常驻服务遇到会话文件缺失、未授权或失效时必须立即退出，禁止在非交互容器中索要手机号、验证码或两步验证密码。

会话文件等同于账号登录权限，必须放在权限受限的宿主持久化目录中，并以可读写方式挂载给唯一的常驻实例；不得写入镜像、代码仓库、CI 产物和日志。账号需要更换或停用时，应先在 Telegram 会话管理中撤销旧会话，再生成新文件。

## 7. URL 与文件识别

### 7.1 URL 提取

依次读取：

- `message.text`。
- `message.caption`。
- `url` entity。
- `text_link` entity 中的隐藏 URL。

URL 标准化后只比较主机名。域名匹配必须满足：

```text
host == configured_domain
or
host ends with "." + configured_domain
```

禁止使用简单字符串后缀判断，避免把 `notyoutube.com` 误判为 `youtube.com`。

### 7.2 文件识别与解析机器人视频关联

- 指定用户的 `video` 和 `video_note` 直接命中候选。
- 指定用户的 `document.mime_type` 以 `video/` 开头时命中候选。
- 机器人视频媒体本身不占固定分组位置，但携带原链接时可作为解析输出参与关联。
- 不根据文件名扩展名猜测 MIME 类型，也不读取或下载文件内容。

### 7.3 纯函数接口

候选识别实现为无副作用纯函数：

```python
def is_video_link_candidate(message: IncomingMessage, domains: frozenset[str]) -> bool:
    ...
```

这样可以独立覆盖 URL、Unicode 域名、隐藏链接和说明文字测试；文件类型由独立媒体判断函数覆盖。

## 8. 状态模型

建议内部模型：

```text
ChatState
  chat_id
  lock
  active_group
  recently_seen_message_ids

MessageGroup
  group_id
  chat_id
  message_count
  candidate_count
  candidate_message_ids
  candidate_url_keys
  closed

CleanupRequest
  chat_id
  message_ids
  reason
  run_id
```

状态规则：

- 每个群组使用独立 `asyncio.Lock`。
- 登记消息、推进固定分组计数、选择保留项和关闭分组在锁内完成。
- Telegram 网络请求全部放到锁外执行。
- 每组最多保存 `X` 条消息中的候选 ID；分组结算后立即释放活动分组。
- 使用有过期时间的消息 ID 集合处理重复更新，保证幂等。
- 使用有过期时间的规范化原链接索引关联解析机器人视频；输出先到时暂存消息 ID，淘汰后到达时立即返回清理请求。
- 清理空闲群组状态和过期幂等记录，避免长期运行后内存持续增长。
- 随机源通过构造参数注入；生产使用系统随机源，测试使用固定随机源。

## 9. 消息处理流程

对每条新群消息按以下顺序处理：

1. 检查功能开关和群组白名单。
2. 排除私聊、频道、服务消息和自动化账号自己的消息；机器人视频进入原链接关联分支且不占分组位置。
3. 取得稳定发送者 ID；匿名发送者按普通非机器人消息计数。
4. 检查消息 ID 是否已经处理。
5. 判断发送者是否为目标用户。
6. 每条非机器人消息将当前组计数加 1。
7. 将目标用户的视频媒体或配置域名 URL 消息登记为候选。
8. 第 `X` 条消息到达时按 `Y` 结算并关闭当前组。
9. 状态服务返回零个或多个待删除消息 ID。
10. 在群组锁外调用清理服务。

状态必须先标记淘汰，再调用 Telegram API。这样即使删除请求较慢，后续消息也能按正确的候选计数继续处理。

## 10. 删除策略

- 优先使用批量删除接口，每批最多 100 个消息 ID。
- 消息已经被用户删除或不存在时按幂等成功处理。
- 捕获 Telethon `FloodWaitError`，按服务端给出的等待秒数休眠后重试。
- 权限不足、消息过旧和非法消息 ID 分别记录结构化错误。
- 单批失败不得阻止其他批次和其他群组继续处理。
- 达到最大重试次数后记录明确失败，不进行无限循环。
- 服务自身不在群内发送成功通知，避免制造额外消息。
- `DRY_RUN=true` 时只记录计划删除的消息，不调用删除接口。

删除失败不会回滚随机状态，因为服务无法可靠恢复已经删除的其他候选。失败项通过日志和指标暴露，由管理员处理权限或限流问题。

## 11. 启动检查

启动实时更新监听前执行：

1. 检查会话文件存在且权限没有开放给其他系统用户。
2. 连接 Telegram 并确认会话已经授权，禁止自动进入交互登录。
3. 调用 `get_me()`，确认 `bot is False`，并确认用户 ID 等于 `TELEGRAM_EXPECTED_USER_ID`。
4. 确认当前账号不在目标用户列表中。
5. 逐个读取受管群组，确认类型为群组或超级群组。
6. 读取当前账号在各群组的参与者权限，确认管理员身份和删除消息权限。
7. 输出当前账号 ID、目标数量、群组数量、分组大小、阈值、域名数量和试运行状态。
8. 不输出 API Hash、会话内容、消息正文、完整 URL 或其他敏感值。

任何受管群组权限不满足时默认启动失败。后续可增加显式的“跳过不可用群组”配置，但不作为首个版本默认行为。

## 12. 可观测性

### 12.1 结构化日志

日志至少包含：

- `event`
- `chat_id`
- `run_id`
- `sender_id`
- `message_id`
- `message_count`
- `candidate_count`
- `retained_message_id`
- `delete_count`
- `dry_run`
- `error_type`

不记录消息正文和完整 URL。用户名仅在启动校验和匹配告警中按需记录。

### 12.2 指标

首个版本记录以下进程内计数器：

- 收到的群消息数。
- 候选消息数。
- 创建的固定分组数。
- 计划删除数。
- 删除成功数。
- 删除失败数。
- 限流重试数。
- 重复更新数。

### 12.3 健康检查

提供轻量 HTTP 接口：

- `/health/live`：事件循环和进程存活。
- `/health/ready`：配置有效、启动检查通过、MTProto 已连接且更新监听任务运行。

健康接口不得暴露配置明文或 Telegram 数据。

## 13. 安全要求

- API Hash 只通过环境变量或容器 secret 注入。
- 会话文件必须使用权限受限的宿主持久化目录注入，并限制为唯一运行用户可读写。
- 使用专用账号，不使用管理员的个人日常账号。
- 专用账号启用两步验证，并定期审查 Telegram 中的活动会话。
- `.env`、日志、缓存和运行数据不得提交。
- 镜像使用非 root 用户运行。
- 容器文件系统尽可能只读，仅会话数据目录和日志目录可写。
- 不开放除健康检查外的网络端口。
- 依赖版本写入锁文件，并在 CI 中执行漏洞检查。
- 日志默认不包含消息内容。
- 删除动作严格限制在配置群组中的目标候选消息，以及携带被淘汰候选规范化原链接的机器人视频消息 ID 内。
- 不使用时间范围批量删除，也不扫描历史消息。

## 14. 建议目录结构

```text
.
├── src/burst_guard/
│   ├── __init__.py
│   ├── __main__.py
│   ├── app.py
│   ├── classifier.py
│   ├── cleanup.py
│   ├── config.py
│   ├── handlers.py
│   ├── health.py
│   ├── login.py
│   ├── models.py
│   ├── preflight.py
│   └── state.py
├── scripts/
│   └── verify_live.py
├── tests/
│   ├── test_classifier.py
│   ├── test_cleanup.py
│   ├── test_config.py
│   ├── test_handlers.py
│   ├── test_preflight.py
│   └── test_state.py
├── .dockerignore
├── .env.example
├── .gitignore
├── Dockerfile
├── docker-compose.yaml
├── pyproject.toml
├── README.md
└── uv.lock
```

## 15. 测试计划

### 15.1 单元测试

- 配置默认值、CSV 标准化和错误提示。
- 用户 ID、用户名、大小写和前导 `@` 匹配。
- 域名精确匹配、子域名匹配和恶意后缀排除。
- 正文、说明文字、普通 URL 和隐藏 URL。
- 目标用户直接视频媒体计入候选，解析机器人视频仍能进入原链接关联。
- 一条消息包含多个 URL 仍只计一次。
- 未满 `X` 条不结算，第 `X` 条到达时按 `Y` 统一结算。
- 候选少于 `Y` 条全部保留，至少 `Y` 条时从该组全部候选中等概率保留 1 条。
- 固定随机源覆盖不同候选保留位置。
- 目标用户普通消息、其他真人和匿名管理员都占用分组位置。
- 多群组状态和相邻固定分组隔离。
- 重复更新不重复计数和删除。
- 解析机器人视频在抽样前到达时随源候选淘汰，延迟到达时立即删除，保留候选的输出不删除。
- 状态压缩和过期记录清理。

### 15.2 处理器集成测试

使用伪造 Telethon 客户端覆盖：

- 群组白名单和非群组过滤。
- 匿名管理员消息计入固定分组。
- 状态锁外执行删除。
- 批量删除分片。
- 已删除消息的幂等处理。
- 权限不足。
- `FloodWaitError` 等待与最大重试次数。
- 试运行模式不调用删除接口。
- 一个群组删除失败不影响另一个群组。

### 15.3 静态检查

- `ruff check .`
- `ruff format --check .`
- `mypy src`
- `pytest`
- Docker 镜像构建。
- Compose 配置校验。

### 15.4 真实群验收

先使用专用测试群和 `DRY_RUN=true`：

- [ ] 专用账号能收到目标用户普通消息和视频消息。
- [ ] 专用账号能识别机器人消息，且机器人消息不占固定分组位置。
- [ ] 启动检查确认管理员删除权限。
- [ ] 固定组未满 `X` 条时只记录、不计划删除。
- [ ] 第 `X` 条非机器人消息到达后，候选达到 `Y` 时恰好计划保留 1 条。
- [ ] 其他真人和目标用户普通文本各占用一个分组位置。
- [ ] 相邻固定分组互不重叠。
- [ ] 不在白名单中的群组完全忽略。

再切换为 `DRY_RUN=false`：

- [ ] 第 `X` 条消息到达后，达到 `Y` 的组内只剩 1 条视频候选。
- [ ] 下一条非机器人消息从新组的第 1 条开始计数。
- [ ] 直接视频、视频便笺、视频文档和配置域名 URL 均计入 `Y`。
- [ ] 用户提前删除候选消息时不会导致清理中断。
- [ ] 权限被撤销后记录明确错误且服务不崩溃。
- [ ] 服务重启后未完成固定分组不会继续计数。

## 16. 实施阶段

- [ ] 1. 建立 Python 项目、依赖锁文件、基础 CI 和目录结构。
- [ ] 2. 完成配置模型、CSV 读取器、会话初始化命令和配置测试。
- [ ] 3. 完成内部消息模型、文件识别和域名识别。
- [ ] 4. 完成固定分组状态服务和等概率随机保留测试。
- [ ] 5. 完成 Telethon 更新处理器和多群组隔离。
- [ ] 6. 完成批量删除、幂等处理、限流重试和试运行模式。
- [ ] 7. 完成启动权限检查、结构化日志和健康接口。
- [ ] 8. 完成 Dockerfile、Compose、示例环境变量和部署文档。
- [ ] 9. 执行单元测试、集成测试、静态检查和镜像检查。
- [ ] 10. 在测试群完成试运行验收。
- [ ] 11. 在测试群开启真实删除并完成全部验收用例。
- [ ] 12. 审查 Git diff，确认无 API Hash、会话文件、群 ID、用户隐私数据和运行产物。
- [ ] 13. 打版本标签并发布首个镜像。

## 17. 完成标准

- 固定组未满 `X` 条时不执行结算。
- 固定组候选少于 `Y` 条时不删除任何候选。
- 固定组候选达到 `Y` 条后，从该组全部候选中等概率保留 1 条。
- 机器人消息不占分组位置，所有非机器人消息各占一个位置。
- 不同群组和相邻固定分组状态不会串扰。
- 重复更新、用户自行删除和 Telegram 限流不会导致服务崩溃。
- 功能默认关闭，首次部署默认试运行。
- 删除范围只能来自已登记的目标候选消息 ID。
- 所有自动化检查通过。
- 真实测试群验收通过。
- 仓库和镜像中不包含生产凭据及私人群组数据。

## 18. 已知限制

- 仅通过域名无法判断一个同时承载图文和视频内容的网站页面是否确实为视频。
- 服务重启会清空当前未完成固定分组。
- 用户名可以变更，数字用户 ID 更可靠。
- Telegram 未投递给服务的消息无法参与固定分组计数。
- 删除能力受 Telegram 权限、消息状态和接口限制约束。
- 单实例内存状态不支持无协调的水平扩展。
- 用户账号自动化可能受到 Telegram 平台规则和风控限制，应使用专用账号、控制请求频率并遵守适用规则。
- 会话文件泄露等同于账号被接管，必须按高敏感凭据管理。
